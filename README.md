# mullva-defender
A Python script that continuously monitors a Mullvad VPN account, automatically logs in if disconnected, detects unauthorized devices, and revokes them in real time using the Mullvad CLI.  The tool also ensures the VPN connection stays active and maintains a clean device list for the account.
